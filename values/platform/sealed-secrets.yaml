# Sealed Secrets controller.
# Keep controller name/namespace aligned with kubeseal defaults.

fullnameOverride: sealed-secrets-controller
namespace: kube-system

replicaCount: 1

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: 200m
    memory: 128Mi

serviceMonitor:
  enabled: false
