apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: argocd
  namespace: infra
spec:
  ingressClassName: traefik
  rules:
    - host: argocd.outegro.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: argocd-server
                port:
                  number: 80
